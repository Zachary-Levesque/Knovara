/**
 * Next.js runtime configuration.
 *
 * This file will configure frontend build behavior, routing options, image
 * handling, environment exposure, and any framework-level settings needed by
 * the Knovara web application.
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {};

export default nextConfig;

